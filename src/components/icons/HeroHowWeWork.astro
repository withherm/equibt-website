---
import handoff from "../../../prototypes/HERO_ICON_HANDOFF.json";

const svg = handoff.components.find((entry) => entry.hero_name === "hero-how-we-work")?.svg ?? "";
---
<Fragment set:html={svg} />
