---
import handoff from "../../../prototypes/HERO_ICON_HANDOFF.json";

const svg = handoff.components.find((entry) => entry.hero_name === "hero-capability-statement")?.svg ?? "";
---
<Fragment set:html={svg} />
