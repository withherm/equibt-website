{/* CTA bookend motif — all-terracotta line enters upper-RIGHT, two gentle curves settle into a calm near-horizontal landing at a bold ring-and-dot waypoint lower-LEFT (arrival, no arrowhead). MIRRORED relative to the raster so the waypoint floats toward the CTA button (Herman 2026-06-06; continuity — the hero thread exits the page's right edge, so re-entering from the right reads as the same line). Terminal logic locked: hero = arrowhead (motion), CTA = waypoint (arrival), do not unify. ViewBox authored tight to the strokes — no internal padding — so the cta-light top alignment holds (EQT-272, look target EQUIBT_V6X_ICON_CANDIDATE_CTA_v2.png). */}
<svg viewBox="0 0 180 104" xmlns="http://www.w3.org/2000/svg">
  <path class="hd-acc" d="M177 3 C163 10 155 21 145 32 C134 44 119 46 105 57 C92 67 78 83 57 85.2 C48 86.1 38.5 86.2 30.5 86.2"/>
  <circle class="hd-acc" cx="17" cy="86.2" r="12"/>
  <circle cx="17" cy="86.2" r="4.8" fill="#C15F3C"/>
</svg>
