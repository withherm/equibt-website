---
import handoff from "../../../prototypes/HERO_ICON_HANDOFF.json";

const svg = handoff.components.find((entry) => entry.hero_name === "hero-industry-hub")?.svg ?? "";
---
<Fragment set:html={svg} />
